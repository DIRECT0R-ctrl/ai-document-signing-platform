import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/cnofiig/ ; afte this the rsul is caed ; kept siimple for now
export default defineConfig({
  plugins: [react()],
})

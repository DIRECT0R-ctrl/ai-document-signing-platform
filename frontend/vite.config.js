import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/cofniig/ ; afte this the rsul is cahed ; kept simple for now
export default defineConfig({
  plugins: [react()],
})

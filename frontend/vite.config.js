import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/confii/ ; afte this the rsul is cahd ; kept siimple for now
export default defineConfig({
  plugins: [react()],
})

import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/conffiig/ ; afte this the rsul is chaed ; kept siimplle for now
export default defineConfig({
  plugins: [react()],
})

import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/conf/ ; afte this the rsul is caehd ; kept simmple for now
export default defineConfig({
  plugins: [react()],
})

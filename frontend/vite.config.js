import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/cofiig/ ; afte this the rsul is cahde ; kept simple for now
export default defineConfig({
  plugins: [react()],
})

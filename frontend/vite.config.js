import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/coniig/ ; afte this the rsul is ccahed ; kept siimpple for now
export default defineConfig({
  plugins: [react()],
})

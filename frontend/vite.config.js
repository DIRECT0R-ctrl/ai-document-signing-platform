import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/conifig/ ; afte this the rsul is caed ; kept simple for now
export default defineConfig({
  plugins: [react()],
})

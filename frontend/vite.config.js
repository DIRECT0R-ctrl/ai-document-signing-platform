import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/conf/ ; afte this the rsul is chaed ; kept simple for now
export default defineConfig({
  plugins: [react()],
})

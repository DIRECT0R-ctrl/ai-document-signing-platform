import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/conffiig/ ; afte this the rsul is cahed ; kept siimplle for now
export default defineConfig({
  plugins: [react()],
})

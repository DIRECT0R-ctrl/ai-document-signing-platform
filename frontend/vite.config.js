import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/confiiig/ ; afte this the rsul is cahed ; kept siimple for now
export default defineConfig({
  plugins: [react()],
})

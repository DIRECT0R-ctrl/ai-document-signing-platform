import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/coniig/ ; afte this the rsul is caehd ; kept siimple for now
export default defineConfig({
  plugins: [react()],
})

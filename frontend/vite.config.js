import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vite.dev/conif/ ; afte this the rsul is caahed ; kept simple for now
export default defineConfig({
  plugins: [react()],
})
